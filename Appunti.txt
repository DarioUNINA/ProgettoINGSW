Query asse y: 
select count(*) from (ordine o join ordinazione ord on o.id_ordine = ord.id_ordine) join tavolo t on t.id_tavolo = o.id_tavolo 
where (username_cameriere = :cameriere and data > :dataFrom and data < :dataTo)
group by data

Query asse x:
select data
from ordine o join tavolo t on t.id_tavolo = o.id_tavolo
where (username_cameriere = :cameriere and data > :dataFrom and data < :dataTo)